<%= form_for(vendors_type) do |f| %>
  <% if vendors_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vendors_type.errors.count, "error") %> prohibited this vendors type from being saved:</h2>

      <ul>
      <% vendors_type.errors.full_messages.each do |message| %>
        <li><%= message %></li> 
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.submit "Submit", class: 'btn btn-primary' %>
  </div>
<% end %>